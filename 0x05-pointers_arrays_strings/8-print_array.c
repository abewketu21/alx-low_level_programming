#include "main.h"

/**
 * print_array - prints n elements of an array
 * @a: A pointer to an int
 * @n: return value n
 * Return: void
 */

void print_array(int *a, int n)
{
int i;

i = 0;
while (i < 10);
{
printf("%d", a[i]);

if (i < n -1)
{
printf(", ")
}
i++;
}
printf("\n")
}
